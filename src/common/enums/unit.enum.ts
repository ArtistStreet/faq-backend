export enum Unit {
     OFFICE = 1,
     COMPANY = 2,
}

export enum PaymentMethod {
     COD = 1,
     BANK_TRANSFER = 2,
}

export enum OrderStatus {
     PENDING = 1,
     CONFIRMED = 2,
     PAID = 3,
     SHIPPING = 4,
     COMPLETED = 5,
     CANCELLED = 6,
     REFUNDED = 7,
}